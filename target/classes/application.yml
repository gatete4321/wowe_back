server:
  port: 9000
spring:
  datasource:
    url: ${vcap.services.wowe.credentials.jdbcUrl}
    username: ${vcap.services.wowe.credentials.username}
    password: ${vcap.services.wowe.credentials.password}
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.mbste.model
  configuration:
    map-underscore-to-camel-case: true


